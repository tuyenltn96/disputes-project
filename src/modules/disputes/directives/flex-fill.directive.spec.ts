import { FlexFillDirective } from './flex-fill.directive';

describe('FlexFillDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexFillDirective();
    expect(directive).toBeTruthy();
  });
});
